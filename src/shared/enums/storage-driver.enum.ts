export enum StorageDriverEnum {
    LOCAL = 'local', // Local file system storage
    MINIO = 'minio', // MinIO is an open-source object storage server compatible with Amazon S3
    GCS = 'gcs', // Google Cloud Storage
}
